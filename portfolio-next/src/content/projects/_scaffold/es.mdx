export const metadata = {
  title: "SCAFFOLD",
  category: "WEB",
  tagline: "",
  blurb: "",
  tags: [],
  size: "sm",
};

Placeholder de compilación — nunca aparece en `PROJECT_SLUGS`, así que
`generateStaticParams`/`dynamicParams=false` lo dejan inalcanzable. Existe
solo para que el import dinámico de MDX tenga al menos un archivo real que
resolver mientras no hay proyectos publicados.
