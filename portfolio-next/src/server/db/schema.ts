// Tablas reales (contacto, comentarios, auth, analytics) se agregan en la
// Fase 6 ("Backend en Next"), portando el modelo de datos ya probado en
// producción en backend/prisma/schema.prisma. Este archivo existe desde la
// Fase 0 solo para que drizzle-kit tenga un punto de entrada válido.
export {};
